<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="@dimen/lag75"
    android:layout_height="@dimen/lag50"
    android:layout_marginStart="@dimen/space.regular"
    android:background="@drawable/card_grad_secondary"
    android:padding="@dimen/space.regular">

    <TextView
        android:id="@+id/tvWeekValue"
        style="@style/TitleText.White"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Week 1" />

    <TextView
        android:id="@+id/tvDayValue"
        style="@style/TitleText.White"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tvWeekValue"
        tools:text="Day 1" />

    <TextView
        android:id="@+id/tvTrainingDesc"
        style="@style/SmallText.White"
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/space.small"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tvDayValue"
        tools:text="Light Jogging Introduction" />

    <TextView
        android:id="@+id/tvTrainingTime"
        style="@style/SmallText.White"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/lag2"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        tools:text="25 min" />

    <ImageView
        android:id="@+id/ivImage"
        android:layout_width="@dimen/lag25"
        android:layout_height="match_parent"
        android:importantForAccessibility="no"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:src="@drawable/im_training_sample" />

</androidx.constraintlayout.widget.ConstraintLayout>